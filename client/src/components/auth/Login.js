import React from 'react';
import Navbar from '../Navbar';
import Header from '../Header';

const Login = () => {
    return (
        <div>
            <Header />
            <Navbar />
            <h1>
                hello login
            </h1>
        </div>

    );
};

export default Login;